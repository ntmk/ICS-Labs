user = int(input('Enter integer (0 - 155): \n'))
flt = float(input('Enter float: \n'))
char = input(('Enter character: \n'))
word = input(('Enter string: \n'))
print(user,flt,char,word)
print(word,char,flt,user)
print(user,'converted to a character is',chr(user))
# FIXME (1): Finish reading other items into variables, then output the four values on a single line separated by a space
   
# FIXME (2): Output the four values in reverse
   
# FIXME (3): Convert the integer to a characer, and output that character